---
'tsargp': minor
---

In addition to the `COMP_LINE` and `COMP_POINT` environment variables, the parsing methods now read default values of the command line and completion index from the `BUFFER` and `CURSOR` variables, respectively.
